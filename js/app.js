function turnCard(card) {
  document.getElementById(card).style.transform = 'rotateY(180deg)';

}
  // limit clicks to two

  // if limit is reached

    // AND contents match add style opacity: 0.5;

      // add one point to score

    // else close open cards and open next card
